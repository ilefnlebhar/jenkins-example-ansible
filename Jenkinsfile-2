pipeline {
  agent { label 'kubmaster70' }
  //environment {
   // ANSIBLE_PRIVATE_KEY=credentials('mariadb-private-key') 
  //}
  stages {
    stage('Hello') {
      steps {
        sh 'ansible-galaxy collection install -r requirements.yml'
        // sh 'echo azer | su - dev7; ansible-playbook -i inventory/mariadb.hosts --private-key=$ANSIBLE_PRIVATE_KEY playbooks/mariadb.yml'
        sh 'ansible-playbook -i inventory/mariadb.hosts playbooks/mariadb.yml -b'

      }
    }
  }
}
